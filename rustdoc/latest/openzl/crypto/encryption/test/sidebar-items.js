window.SIDEBAR_ITEMS = {"fn":[["correctness","Tests if encryption of `plaintext` using `encryption_key` and `randomness` returns the original `plaintext` on decryption using `decryption_key`. The `assert_same` function is used to assert that the two plaintexts are the same."],["correctness_with_derive","Derives an `EncryptionKey` from `decryption_key` and then runs the [`correctness`] assertion test."]]};