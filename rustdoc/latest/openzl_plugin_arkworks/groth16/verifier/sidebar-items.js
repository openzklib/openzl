window.SIDEBAR_ITEMS = {"fn":[["prepare_inputs","Prepare proof inputs for use with [`verify_proof_with_prepared_inputs`], wrt the prepared verification key `pvk` and instance public inputs."],["prepare_verifying_key","Prepare the verifying key `vk` for use in proof verification."],["verify_proof","Verify a Groth16 proof `proof` against the prepared verification key `pvk`, with respect to the instance `public_inputs`."],["verify_proof_with_prepared_inputs","Verify a Groth16 proof `proof` against the prepared verification key `pvk` and prepared public inputs. This should be preferred over [`verify_proof`] if the instance’s public inputs are known in advance."]]};